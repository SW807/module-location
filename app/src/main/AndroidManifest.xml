<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dk.aau.cs.psylog.location" >
    <uses-permission android:name="dk.aau.cs.psylog.psylog.READ_DB"/>
    <uses-permission android:name="dk.aau.cs.psylog.psylog.WRITE_DB"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <service
            android:name="dk.aau.cs.psylog.location.PsyLogService"
            android:enabled="true"
            android:process=":my_process"
            android:exported="true" >
        </service>

        <activity
            android:name="dk.aau.cs.psylog.location.MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <uses-feature android:name="android.hardware.location" android:required="true" />
    </application>

</manifest>
